# Whitelist: ignore everything, track only what is explicitly negated below.
# ~/.claude is a live runtime directory (sessions, caches, credentials) —
# never flip this to a blocklist.
/*
!/.gitignore
!/README.md
!/.claude-plugin/
!/output-styles/
